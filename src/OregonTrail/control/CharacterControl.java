/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package OregonTrail.control;

import OregonTrail.model.Character;
import java.util.ArrayList;

/**
 *
 * @author Kim Llanos & Jocelyn Tucker
 */
public class CharacterControl {

    /*
    *****Changed to enum*****
    public static ArrayList<Character> createCharacters() {
        ArrayList<Character> characters = new ArrayList<>();//actors = create an array Actor objects

        Character playersCharacter = new Character(OregonTrail.currentPlayer.getName(), 2, 2, 1, true, 100, 2); //actor1 ****** Need to retrieve player's name correctly
        characters.add(playersCharacter);//Assign actor1 to the next position in the actors array

        Character bob = new Character("Bob", 1, 1, 2, true, 100, 1); //actor2
        characters.add(bob);//Assign actor2 to the next position in the actors array

        Character sarah = new Character("Sarah", 3, 1, 1, true, 100, 3); //actor3
        characters.add(sarah);//Assign actor3 to the next position in the actors array

        Character tom = new Character("Tom", 2, 0, 2, true, 100, 2); //actor4
        characters.add(tom);//Assign actor4 to the next position in the actors array

        Character rebecca = new Character("Rebecca", 2, 2, 0, true, 100, 2); //actor5
        characters.add(rebecca);//Assign actor5 to the next position in the actors array

        Character jorge = new Character("Jorge", 3, 2, 2, true, 100, 3); //actor6
        characters.add(jorge);//Assign actor6 to the next position in the actors array

        return characters;
    }*/
}
